# Report Usage And Remaining Capacity

```text
Use $joinlayer-pipelines. Report current JoinLayer capacity and usage for the
date range I provide. Verify the workspace and exact returned time boundaries.
Separate current billing-period admission limits from the historical usage
report, and show used, remaining, and limit values independently. Break down
usage by pipeline when available. Explain any blocker or approaching limit and
whether waiting for a period reset can resolve it. Do not change anything.
```

Completion evidence: workspace, current billing-period boundaries, requested
report boundaries, independent capacity dimensions, per-pipeline attribution
when returned, and an explicit statement that no state changed.
