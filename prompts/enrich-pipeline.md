# Enrich A Pipeline

```text
Use $joinlayer-pipelines. Add lookup enrichment to the pipeline I identify.
Inspect its current revision and discover the lookup connection schema. Ask me
to resolve ambiguous join keys or duplicate-match behavior. Update only the
draft, then validate and preview representative matched and unmatched rows.
Do not run the pipeline.
```
