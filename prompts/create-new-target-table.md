# Copy Into A New Destination Table

```text
Use $joinlayer-pipelines. Copy the source table I identify into a new
destination table. Discover both connector capabilities and source schema
before drafting. Confirm that the target supports managed table creation,
show the proposed destination schema and write strategy, and explain when the
table will be created. Validate and preview the draft. Do not start a run until
I review the preview and the exact target effect.
```

Completion evidence: discovered source coordinates, target table-creation capability, proposed schema, saved pipeline ID, successful validation, and preview row counts.
